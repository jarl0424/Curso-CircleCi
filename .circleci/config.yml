version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout

      - run:
          name: Ver repo
          command: |
            ls -la
            echo "CircleCI funcionando OK"

workflows:
  test-workflow:
    jobs:
      - test